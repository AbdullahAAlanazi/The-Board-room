# Copy this file to ".env" and put your real key in.
# .env is git-ignored, so your key never gets committed.

OPENAI_API_KEY=sk-your-key-here

# Model the advisors and chairman use.
# gpt-4o-mini is cheap + fast for building; switch to gpt-4o for quality.
OPENAI_MODEL=gpt-4o-mini
