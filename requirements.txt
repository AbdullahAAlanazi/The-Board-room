# --- AI Board Room: dependencies ---

langchain              # core chains / prompt + LLM plumbing
langchain-openai       # OpenAI chat model + embeddings
langchain-community    # extra integrations (document loaders, retrievers)
pydantic               # the shared data schema (the contract)
pydantic-settings      # load config from .env
python-dotenv          # read the .env file
faiss-cpu              # local vector store (RAG)
pypdf                  # PDF loader
fastapi                # REST API framework
uvicorn[standard]      # ASGI server
python-multipart       # multipart/form-data (file uploads)
aiofiles               # required by FastAPI StaticFiles
