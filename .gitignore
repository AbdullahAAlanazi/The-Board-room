# Secrets — never commit real keys
.env

# Python
__pycache__/
*.py[cod]
.venv/

# OS / editor
.DS_Store
.vscode/
.idea/

# Company documents — real PDFs should not live in git
documents/*.pdf

# FAISS index cache — derived from documents, rebuilt automatically
.rag_cache/
